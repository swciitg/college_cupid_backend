const POOL = "pool";

let boys = [];
let girls = [];
const rooms = {};

const CHAT_TIME = 3 * 60 * 1000;

const MIN_SCORE = 5;

module.exports = {
  boys,
  girls,
  rooms,
  POOL,
  CHAT_TIME,
  MIN_SCORE
};
