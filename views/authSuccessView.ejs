<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CollegeCupid</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            height: 100vh;
            background-color: #ffffff;
            background-size: cover;
            font-family: 'Montserrat';
            color: #000000;
        }
        .splash-text p {
            margin: 0;
            font-size: 18px;
        }

        .splash-text div {
            font-family: 'Sedgwick Ave';
            font-weight: bold;
            font-size: 50px;
        }
    </style>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400&family=Sedgwick+Ave&display=swap" rel="stylesheet">
</head>
<body>
    <div class="splash-text">
        <img src="<%= basePath %>/assets/text_icon.png" alt="CollegeCupid" style="max-width: 80%; height: auto;">
        <!-- <div>CollegeCupid</div> -->
        <div style="height: 10px;"></div>
        <% if(status==="ERROR"){ %>
            <p>Oops!! Something went wrong :(</p>
            <p>Please try again</p>
        <% } else {%>
            <p>Successfully signed in.</p>
            <p>Redirecting to College Cupid...</p>
        <%}%>
        <p id="status" hidden><%= status %></p>
        <p id="outlookInfo" hidden><%= outlookInfo %></p>
    </div>

    <!-- In your authSuccessView.ejs, replace the script section with: -->
    <script>
        setTimeout(() => {
            const status = '<%= status %>';
            // Use JSON.stringify in the template to ensure proper escaping
            const outlookInfo = <%- JSON.stringify(outlookInfo) %>;

            // Create URL parameters
            const params = new URLSearchParams();
            params.append('status', status);
            params.append('outlookInfo', JSON.stringify(outlookInfo));

            // Redirect to React app
            window.location.href = 'http://localhost:3000/auth/callback?' + params.toString();
        }, 2000);
    </script>
</body>
</html>
